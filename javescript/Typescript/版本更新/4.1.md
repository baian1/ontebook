# 4.1

1. 模版字符串
    - 基础用法

   ```ts
   type Color = "red" | "blue";
   type Quantity = "one" | "two";

   type SeussFish = `${Quantity | Color} fish`;
   //type SeussFish = "one fish" | "two fish" | "red fish" | "blue fish"
   ```

   - 类型别名Uppercase, Lowercase, Capitalize and Uncapitalize
2. Key Remapping in Mapped Types
    对key进行重建 ,可以过滤或变更key

   ```ts
   type MappedTypeWithNewKeys<T> = {
        [K in keyof T as NewKeyType]: T[K]
        //            ^^^^^^^^^^^^^
        //            This is the new syntax!
    }
   ```

   ```ts
    // Remove the 'kind' property
    type RemoveKindField<T> = {
        [K in keyof T as Exclude<K, "kind">]: T[K]
    };

    interface Circle {
        kind: "circle";
        radius: number;
    }

    type KindlessCircle = RemoveKindField<Circle>;

    //生成新key
    type Getters<T> = {
        [K in keyof T as `get${Capitalize<string & K>}`]: () => T[K]
    };

    type LazyCircle = Getters<Circle>;
   ```

3. noUncheckedIndexedAccess
   1. 数组里的越界元素需要新增对undefined判断
   2. 对象里的 非显示声明的 string/Symbol key 属性添加 undefined
4. Recursive Conditional Types 递归条件类型