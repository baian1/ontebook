# 4.3

1. 添加对set与get的支持

   ```ts
   // Now valid!
    interface Thing {
        get size(): number
        set size(value: number | string | boolean);
    }
   ```

2. `override` and the `--noImplicitOverride` Flag,当子类扩展父类,并覆盖方法时,添加 override 关键字表示覆盖父元素方法
3. `ConstructorParameters` 可以用于Abstract Classes
4. Contextual Narrowing for Generics 待看
